//trong 1 file chỉ export default 'duy nhất 1' đối tượng Fn duy nhất và khi import thì ko cần để trong { }, và có thể đặt tên khác

// còn export thì có thể cho nhiều function // khi import cần pải đúng tên và để trong { }// nếu muốn đổi tên thì  dùng ' as ' 